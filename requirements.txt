# Core web UI and server
gradio
# WebSocket support for uvicorn — browser mic/tab audio streams over /ws/.
# Recent Gradio releases no longer pull this in, and without it every audio
# WebSocket is rejected with a 404.
websockets

# Speech recognition
vosk
sounddevice
moonshine-voice                  # ONNX‑based local ASR (auto‑downloads models)

# Audio & VAD
webrtcvad                        # Voice Activity Detection (requires compiler)
cffi                             # Required by webrtcvad

# Data & numeric processing
numpy

# HTTP requests (Whisper API, translation backends)
requests>=2.31.0

# Translation backends
translators                       # Google Translate, etc. (online)
argostranslate                   # Offline translation (Argos)
tokenizers                       # Used by some HuggingFace models
huggingface_hub                  # For model downloads (Moonshine, etc.)